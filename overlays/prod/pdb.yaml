apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: infrastore
  namespace: infrastore
spec:
  minAvailable: 2  # Keep at least 2 pods running (increased from 1)
  selector:
    matchLabels:
      app: infrastore
  unhealthyPodEvictionPolicy: AlwaysAllow
